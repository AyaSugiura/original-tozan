<%= form_for(@photo) do |f| %>


　　<%= f.label :image %>
　　<%= f.file_field :image %> （画像用に追加したカラムの名前）

    <%= f.label :comment %>
    <%= f.text_field :comment %>
    
    <%= f.submit "投稿",class: 'btn-primary btn-block' %>
<% end %>
